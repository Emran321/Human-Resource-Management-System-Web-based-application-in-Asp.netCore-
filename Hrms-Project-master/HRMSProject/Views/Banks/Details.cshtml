@model HRMSProject.Models.VmBank

@{
    ViewData["Title"] = "Details";
}
<div class="container">
    <h1>Details</h1>
    <div>
        <h4>Employee Type</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.BankId)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.BankId)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.BankName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.BankName)
            </dd>

        </dl>
    </div>
    <div>
        <a asp-controller="EmployeeTypes" asp-action="Edit" asp-route-id="@Model.BankId" class="btn btn-primary">Edit</a>
        <a asp-action="Index" class="btn btn-danger">Back to List</a>
    </div>
</div>